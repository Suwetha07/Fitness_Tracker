apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: suwetha-gateway
spec:
  gatewayClassName: kgateway
  Listeners:
    - name: http
      protocol: HTTP
      port: 80
      routes:
        allowedRoutes:
          namespaces:
            from: Same