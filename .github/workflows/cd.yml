name: fitness-tracker-app-cd

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name:  Code
        uses: actions/checkout@v3

      - name: Message
        run: echo "Simple"